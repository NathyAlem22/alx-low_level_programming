#include <stdio.h>

/**
 * main - prints every number from 0 to 10
 * Return: 0 for success
 */
int main(void)
{
	int num;

	while (num < 10)
	{
		printf("%d", num);
		num++;

	}
	printf('\n');
	return (0);
}
