#include <stdio.h>
/**
 * main - is the entry point
 * printf() function sends a formatted string to the standard out
 * outcome very good,
 * Return: 0 when the program runs without error
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
